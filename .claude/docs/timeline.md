# Git Timeline

- Each branch gets its own color, main is always the same color (user-definable, default in other file)
- ## Each commit is organized chronologically, either from top to bottom or bottom to top.
